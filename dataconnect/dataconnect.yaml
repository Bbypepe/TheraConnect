specVersion: "v1beta"
serviceId: "TheraConnect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "TheraConnect-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
